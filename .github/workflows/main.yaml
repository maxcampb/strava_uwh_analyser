
name: "Github workflow"
on:
  push:
    branches: "main"
  pull_request:
    branches: "*"
  workflow_dispatch:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Python
        uses: actions/setup-python@v1
        with:
          python-version: "3.8"

      - name: Setup Invoke
        run: pip install invoke

      - name: Install Requirements
        run: inv build

      - name: Checkout Helper Max repository
        uses: actions/checkout@v2
        with:
          repository:

